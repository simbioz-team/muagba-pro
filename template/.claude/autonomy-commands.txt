# Рутинные команды этапа — по одной на строку, в том виде, в каком их
# выполнит агент (со склейками через && и ;, если он так делает).
# Перед автономной сессией: /muagba-base:preflight покажет, какие из них
# упрутся в подтверждение человека.
#
# <допиши команды проекта>
./.claude/check.sh
git status --short
git add -A
git commit -m "сообщение"
git worktree list
git worktree remove .claude/worktrees/<дерево>
git branch -d <ветка-дерева>
git worktree prune
