# Пути, которые агент не может править и не может удалить. Одна строка —
# один шаблон. Сверка по подстроке, но только на границе пути: '.env' ловит
# '.env' и '.env.local', но не 'os.environ' и не 'src/environment.ts'.
# Читается хуком PreToolUse плагина muagba-base.
# Префикс '+' — «записывается один раз»: создать файл можно, изменить уже
# созданный нельзя.
# Дублирует часть deny-правил из settings.json намеренно: правило работает
# на файловых инструментах, хук — ещё и когда путь приходит иначе.

.env
# Исключение: образец переменных заполняет агент на Э5. Правило '.env'
# ловит по подстроке, и без этой строки '.env.example' тоже под запретом.
!.env.example
.git/
# lock-файлы: их меняет менеджер зависимостей, не агент
package-lock.json
uv.lock
poetry.lock
yarn.lock
pnpm-lock.yaml
# Документы рамок правит человек — НО только после того, как они заполнены.
# Включённые с самого начала, эти строки запрещают ровно ту работу,
# которую предписывают этапы Э3 и Э4: агент не может дописать конституцию,
# которую его же попросили составить. Раскомментировать на Э11 ВСЕ, вместе со
# снятием --soft. Это проверяет проба dry.strict.
# docs/constitution.md
# docs/tech-stack.md
# docs/structure.md
# Критерии готовности и рабочий цикл — то, чем агент отчитывается о себе.
# Без них защита неполна: конституция под замком, а «что считается готовым»
# агент может переписать себе сам и после этого честно доложить, что готово.
# docs/definition-of-done.md
# docs/workflow.md
# docs/lessons.md сюда НЕ входит намеренно: журнал уроков агенты пополняют,
# это его назначение, и проба observe.rule ждёт там проектных записей.
# принесённые материалы: свидетельство, а не документ проекта.
# Положить можно, переписать нельзя: правки идут в документы рамок,
# источник остаётся тем, чем был. См. ADR-0005 базы.
+docs/sources/
