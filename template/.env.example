# Образец окружения. Скопируй в .env и заполни.
# Сам .env в .gitignore и в deny-правилах: агент его не читает и не правит.
# Перечисли здесь ВСЕ переменные, которые нужны проекту, — без значений.

# EXAMPLE_API_KEY=
