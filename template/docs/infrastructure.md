---
title: Инфраструктура
scope: product
type: frame
status: draft
updated: 2026-09-20
tags: [среда, инфраструктура, секреты]
summary: Окружения, сервисы, секреты и изоляция агентов. Отвечает на вопрос, из чего собирается работающее приложение.
---

# Инфраструктура

<Если у проекта нет сервисов, окружений и деплоя — удали этот файл и убери
ссылку на него из AGENTS.md. Пустой документ хуже отсутствующего.>

## Окружения

| Окружение | Где | Кто имеет доступ |
|---|---|---|
| local | <> | <> |
| <> | <> | <> |

## Сервисы

<Что должно быть поднято, чтобы проект заработал: БД, брокер, кеш.
Версии и способ запуска.>

## Секреты

Список переменных — в `.env.example`, **без значений**. Сам `.env` в
`.gitignore`, в deny-правилах `settings.json` и в `.worktreeinclude`
(чтобы копировался в рабочие деревья агентов).

Где лежат настоящие значения: <>

## Изоляция агентов

<!-- no-question: решается на Э8, здесь справка -->

Рабочие деревья и `.worktreeinclude` настраиваются на Э8, см.
`docs/workflow.md`.

Про контейнер: Claude Code ставится в dev-контейнер через
`ghcr.io/anthropics/devcontainer-features/claude-code:1.0`. Оправдан, когда
есть сервисы, несколько машин или нужно ограничить агенту доступ к хосту.
Для локального однопользовательского проекта избыточен.

## Деплой

<!-- no-question: за пределами конвейера настройки -->

<Как выкатывается. Что делать при откате. Конвейер настройки доводит проект
до первой фичи и про выкатку не спрашивает: до неё ещё нечего выкатывать.>
